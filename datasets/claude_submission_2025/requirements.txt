# Project Starlight - Model Requirements
# Claude's Submission 2025

# Core inference dependencies (minimal, for deployment)
onnxruntime>=1.16.0
Pillow>=9.0.0
numpy>=1.21.0

# Training dependencies (optional, only needed for model development)
# Uncomment if retraining:
# torch>=2.0.0
# torchvision>=0.15.0
# tqdm>=4.65.0
# onnx>=1.14.0

# Note: For ensemble aggregation, only onnxruntime + Pillow + numpy are required
